services:
  - type: web
    name: majin-studios
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npx serve@latest out
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
